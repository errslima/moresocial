-- Executed as separate statements by psql (never combine into a single -c).
-- Variables are SQL identifiers, quoted by psql, not interpolated shell fragments.
CREATE ROLE :"restore_owner";
CREATE ROLE :"restore_role";
CREATE DATABASE :"restore_db";
